export enum RoleAction {
  None = "none",
  Peek = "peek",
  SwapSelf = "swapSelf",
  SwapTwo = "swapTwo",
  SwapCenter = "swapCenter",
}

export enum TargetType {
  Self = "self",
  Player = "player",
  Center = "center",
}
